# whatsUp
Comprehensive tool for scheduling nighttime observations

cd to /var/www/html
Run: git clone github.com/sifank/whatsUp
Move whatsUp.conf to /etc/apache2/sites-available
Run:  a2ensite whatsUp
Run:  service apache2 restart
